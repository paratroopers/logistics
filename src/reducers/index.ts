/**
 * define redux reducers
 * don't combine reducers at here which will be implemented in baseApp, just return reducers in Object
 * e.g.
 * import {category} from "./category";
 * export let reducers = {category}
 **/